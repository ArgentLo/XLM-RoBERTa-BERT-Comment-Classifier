## BERT and XLM-RoberTa Model for Text Classification

- Configure model hyper-parameters in `config.py`.
- Starting training process by running `train_<model_name>.py`.
- Evaluation with `eval_<model_name>.py`.


